import React from "react"
import ReactDOM from "react-dom"
import D3Events from "./D3Events"

ReactDOM.render(
  <React.StrictMode>
    <D3Events />
  </React.StrictMode>,
  document.getElementById("root")
)
